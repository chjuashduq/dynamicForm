# QML 趣味示例集合 🎮

## 📚 目录
1. [游戏示例](#游戏示例)
2. [动画效果](#动画效果)
3. [交互组件](#交互组件)
4. [创意项目](#创意项目)

---

## 游戏示例

### 1. 打地鼠游戏 🔨

```qml
import QtQuick 6.5
import QtQuick.Controls 6.5

ApplicationWindow {
    width: 600
    height: 600
    visible: true
    title: "打地鼠游戏"
    
    property int score: 0
    property int timeLeft: 30
    
    Rectangle {
        anchors.fill: parent
        color: "#8BC34A"
        
        // 分数和时间
        Row {
            anchors.top: parent.top
            anchors.horizontalCenter: parent.horizontalCenter
            anchors.topMargin: 20
            spacing: 50
            
            Text {
                text: "分数: " + score
                font.pixelSize: 24
                font.bold: true
            }
            
            Text {
                text: "时间: " + timeLeft + "s"
                font.pixelSize: 24
                font.bold: true
                color: timeLeft < 10 ? "red" : "black"
            }
        }
        
        // 地鼠洞
        Grid {
            anchors.centerIn: parent
            columns: 3
            rows: 3
            spacing: 20
            
            Repeater {
                model: 9
                
                Rectangle {
                    width: 150
                    height: 150
                    color: "#795548"
                    radius: 75
                    border.color: "#5D4037"
                    border.width: 5
                    
                    property bool moleVisible: false
                    
                    // 地鼠
                    Rectangle {
                        id: mole
                        width: 100
                        height: 100
                        anchors.centerIn: parent
                        color: "#8D6E63"
                        radius: 50
                        visible: parent.moleVisible
                        
                        // 眼睛
                        Row {
                            anchors.centerIn: parent
                            anchors.verticalCenterOffset: -10
                            spacing: 20
                            
                            Rectangle {
                                width: 15
                                height: 15
                                radius: 7.5
                                color: "black"
                            }
                            
                            Rectangle {
                                width: 15
                                height: 15
                                radius: 7.5
                                color: "black"
                            }
                        }
                        
                        // 鼻子
                        Rectangle {
                            width: 10
                            height: 10
                            radius: 5
                            color: "pink"
                            anchors.centerIn: parent
                            anchors.verticalCenterOffset: 10
                        }
                        
                        // 点击效果
                        MouseArea {
                            anchors.fill: parent
                            onClicked: {
                                score += 10
                                parent.parent.moleVisible = false
                                hitAnimation.start()
                            }
                        }
                        
                        // 被击中动画
                        SequentialAnimation {
                            id: hitAnimation
                            NumberAnimation {
                                target: mole
                                property: "scale"
                                to: 1.5
                                duration: 100
                            }
                            NumberAnimation {
                                target: mole
                                property: "scale"
                                to: 1.0
                                duration: 100
                            }
                        }
                    }
                    
                    // 随机出现地鼠
                    Timer {
                        interval: Math.random() * 2000 + 1000
                        running: timeLeft > 0
                        repeat: true
                        onTriggered: {
                            parent.moleVisible = true
                            hideTimer.start()
                        }
                    }
                    
                    // 自动隐藏
                    Timer {
                        id: hideTimer
                        interval: 1000
                        onTriggered: parent.moleVisible = false
                    }
                }
            }
        }
        
        // 游戏计时器
        Timer {
            interval: 1000
            running: timeLeft > 0
            repeat: true
            onTriggered: {
                timeLeft--
                if (timeLeft === 0) {
                    gameOverDialog.open()
                }
            }
        }
        
        // 游戏结束对话框
        Dialog {
            id: gameOverDialog
            anchors.centerIn: parent
            title: "游戏结束"
            modal: true
            
            Column {
                spacing: 20
                
                Text {
                    text: "你的分数: " + score
                    font.pixelSize: 24
                }
                
                Button {
                    text: "再玩一次"
                    anchors.horizontalCenter: parent.horizontalCenter
                    onClicked: {
                        score = 0
                        timeLeft = 30
                        gameOverDialog.close()
                    }
                }
            }
        }
    }
}
```

**学习要点:**
- Timer 的使用
- 随机数生成
- 动画效果
- 游戏逻辑

---

### 2. 贪吃蛇游戏 🐍

```qml
import QtQuick 6.5
import QtQuick.Controls 6.5

ApplicationWindow {
    width: 600
    height: 600
    visible: true
    title: "贪吃蛇"
    
    property int gridSize: 20
    property int cellSize: 30
    property var snake: [[10, 10], [10, 11], [10, 12]]
    property var direction: [0, -1]  // 上
    property var food: [15, 15]
    property int score: 0
    property bool gameOver: false
    
    Rectangle {
        anchors.fill: parent
        color: "#2C3E50"
        focus: true
        
        Keys.onPressed: function(event) {
            if (gameOver) return
            
            switch(event.key) {
                case Qt.Key_Up:
                    if (direction[1] !== 1) direction = [0, -1]
                    break
                case Qt.Key_Down:
                    if (direction[1] !== -1) direction = [0, 1]
                    break
                case Qt.Key_Left:
                    if (direction[0] !== 1) direction = [-1, 0]
                    break
                case Qt.Key_Right:
                    if (direction[0] !== -1) direction = [1, 0]
                    break
            }
        }
        
        // 分数
        Text {
            anchors.top: parent.top
            anchors.horizontalCenter: parent.horizontalCenter
            anchors.topMargin: 10
            text: "分数: " + score
            font.pixelSize: 24
            color: "white"
            font.bold: true
        }
        
        // 游戏区域
        Rectangle {
            id: gameArea
            anchors.centerIn: parent
            width: gridSize * cellSize
            height: gridSize * cellSize
            color: "#34495E"
            border.color: "#1ABC9C"
            border.width: 2
            
            // 蛇身
            Repeater {
                model: snake.length
                
                Rectangle {
                    x: snake[index][0] * cellSize
                    y: snake[index][1] * cellSize
                    width: cellSize - 2
                    height: cellSize - 2
                    color: index === 0 ? "#E74C3C" : "#3498DB"
                    radius: 4
                    
                    // 蛇头的眼睛
                    Row {
                        visible: index === 0
                        anchors.centerIn: parent
                        spacing: 8
                        
                        Rectangle {
                            width: 6
                            height: 6
                            radius: 3
                            color: "white"
                        }
                        
                        Rectangle {
                            width: 6
                            height: 6
                            radius: 3
                            color: "white"
                        }
                    }
                }
            }
            
            // 食物
            Rectangle {
                x: food[0] * cellSize
                y: food[1] * cellSize
                width: cellSize - 2
                height: cellSize - 2
                color: "#F39C12"
                radius: (cellSize - 2) / 2
                
                // 闪烁效果
                SequentialAnimation on opacity {
                    loops: Animation.Infinite
                    NumberAnimation { to: 0.3; duration: 500 }
                    NumberAnimation { to: 1.0; duration: 500 }
                }
            }
        }
        
        // 游戏循环
        Timer {
            interval: 150
            running: !gameOver
            repeat: true
            onTriggered: {
                // 移动蛇
                var head = snake[0]
                var newHead = [
                    head[0] + direction[0],
                    head[1] + direction[1]
                ]
                
                // 检查碰撞
                if (newHead[0] < 0 || newHead[0] >= gridSize ||
                    newHead[1] < 0 || newHead[1] >= gridSize) {
                    gameOver = true
                    gameOverDialog.open()
                    return
                }
                
                // 检查是否吃到自己
                for (var i = 0; i < snake.length; i++) {
                    if (snake[i][0] === newHead[0] && snake[i][1] === newHead[1]) {
                        gameOver = true
                        gameOverDialog.open()
                        return
                    }
                }
                
                // 添加新头
                snake.unshift(newHead)
                
                // 检查是否吃到食物
                if (newHead[0] === food[0] && newHead[1] === food[1]) {
                    score += 10
                    // 生成新食物
                    food = [
                        Math.floor(Math.random() * gridSize),
                        Math.floor(Math.random() * gridSize)
                    ]
                } else {
                    // 移除尾巴
                    snake.pop()
                }
                
                // 强制刷新
                snake = snake.slice()
            }
        }
        
        // 游戏结束对话框
        Dialog {
            id: gameOverDialog
            anchors.centerIn: parent
            title: "游戏结束"
            modal: true
            
            Column {
                spacing: 20
                
                Text {
                    text: "你的分数: " + score
                    font.pixelSize: 24
                }
                
                Button {
                    text: "重新开始"
                    anchors.horizontalCenter: parent.horizontalCenter
                    onClicked: {
                        snake = [[10, 10], [10, 11], [10, 12]]
                        direction = [0, -1]
                        food = [15, 15]
                        score = 0
                        gameOver = false
                        gameOverDialog.close()
                    }
                }
            }
        }
        
        // 提示
        Text {
            anchors.bottom: parent.bottom
            anchors.horizontalCenter: parent.horizontalCenter
            anchors.bottomMargin: 20
            text: "使用方向键控制"
            font.pixelSize: 16
            color: "#95A5A6"
        }
    }
}
```

**学习要点:**
- 键盘事件处理
- 数组操作
- 碰撞检测
- 游戏状态管理

---

## 动画效果

### 3. 粒子爆炸效果 💥

```qml
import QtQuick 6.5
import QtQuick.Particles

ApplicationWindow {
    width: 800
    height: 600
    visible: true
    title: "粒子效果"
    
    Rectangle {
        anchors.fill: parent
        color: "#1a1a1a"
        
        ParticleSystem {
            id: particleSystem
            
            ImageParticle {
                source: "qrc:///particleresources/glowdot.png"
                color: "#FFD700"
                colorVariation: 0.6
                alpha: 0.8
            }
            
            Emitter {
                id: emitter
                anchors.centerIn: parent
                width: 1
                height: 1
                
                emitRate: 0
                lifeSpan: 2000
                lifeSpanVariation: 500
                
                velocity: AngleDirection {
                    angle: 0
                    angleVariation: 360
                    magnitude: 200
                    magnitudeVariation: 100
                }
                
                size: 16
                sizeVariation: 8
            }
        }
        
        MouseArea {
            anchors.fill: parent
            onClicked: function(mouse) {
                emitter.x = mouse.x
                emitter.y = mouse.y
                emitter.burst(100)
            }
        }
        
        Text {
            anchors.centerIn: parent
            text: "点击任意位置产生粒子效果"
            font.pixelSize: 24
            color: "white"
            opacity: 0.5
        }
    }
}
```

---

### 4. 波纹效果 🌊

```qml
import QtQuick 6.5

ApplicationWindow {
    width: 600
    height: 600
    visible: true
    title: "波纹效果"
    
    Rectangle {
        anchors.fill: parent
        color: "#0077BE"
        
        Repeater {
            id: ripples
            model: 0
            
            Rectangle {
                id: ripple
                width: 0
                height: 0
                radius: width / 2
                color: "transparent"
                border.color: "white"
                border.width: 3
                opacity: 1.0
                
                property real centerX: 0
                property real centerY: 0
                
                x: centerX - width / 2
                y: centerY - height / 2
                
                ParallelAnimation {
                    running: true
                    
                    NumberAnimation {
                        target: ripple
                        property: "width"
                        to: 400
                        duration: 1500
                        easing.type: Easing.OutQuad
                    }
                    
                    NumberAnimation {
                        target: ripple
                        property: "height"
                        to: 400
                        duration: 1500
                        easing.type: Easing.OutQuad
                    }
                    
                    NumberAnimation {
                        target: ripple
                        property: "opacity"
                        to: 0
                        duration: 1500
                    }
                    
                    onFinished: {
                        ripples.model = Math.max(0, ripples.model - 1)
                    }
                }
            }
        }
        
        MouseArea {
            anchors.fill: parent
            onClicked: function(mouse) {
                var component = Qt.createComponent("qrc:/Ripple.qml")
                if (component.status === Component.Ready) {
                    var ripple = component.createObject(parent, {
                        centerX: mouse.x,
                        centerY: mouse.y
                    })
                }
                ripples.model++
            }
        }
        
        Text {
            anchors.centerIn: parent
            text: "点击产生波纹"
            font.pixelSize: 32
            color: "white"
            opacity: 0.7
        }
    }
}
```

---

## 交互组件

### 5. 3D 旋转卡片 🎴

```qml
import QtQuick 6.5
import QtQuick.Controls 6.5

ApplicationWindow {
    width: 800
    height: 600
    visible: true
    title: "3D 卡片效果"
    
    Rectangle {
        anchors.fill: parent
        
        gradient: Gradient {
            GradientStop { position: 0.0; color: "#667eea" }
            GradientStop { position: 1.0; color: "#764ba2" }
        }
        
        Row {
            anchors.centerIn: parent
            spacing: 50
            
            Repeater {
                model: 3
                
                Item {
                    width: 200
                    height: 300
                    
                    property bool flipped: false
                    
                    Rectangle {
                        id: card
                        anchors.fill: parent
                        radius: 15
                        
                        transform: [
                            Rotation {
                                id: rotation
                                origin.x: card.width / 2
                                origin.y: card.height / 2
                                axis { x: 0; y: 1; z: 0 }
                                angle: 0
                            },
                            Scale {
                                id: scale
                                origin.x: card.width / 2
                                origin.y: card.height / 2
                                xScale: 1.0
                                yScale: 1.0
                            }
                        ]
                        
                        // 正面
                        Rectangle {
                            anchors.fill: parent
                            radius: 15
                            visible: rotation.angle < 90
                            
                            gradient: Gradient {
                                GradientStop { position: 0.0; color: "#f093fb" }
                                GradientStop { position: 1.0; color: "#f5576c" }
                            }
                            
                            Column {
                                anchors.centerIn: parent
                                spacing: 20
                                
                                Text {
                                    text: "🎨"
                                    font.pixelSize: 64
                                    anchors.horizontalCenter: parent.horizontalCenter
                                }
                                
                                Text {
                                    text: "卡片 " + (index + 1)
                                    font.pixelSize: 24
                                    color: "white"
                                    font.bold: true
                                }
                            }
                        }
                        
                        // 背面
                        Rectangle {
                            anchors.fill: parent
                            radius: 15
                            visible: rotation.angle >= 90
                            
                            gradient: Gradient {
                                GradientStop { position: 0.0; color: "#4facfe" }
                                GradientStop { position: 1.0; color: "#00f2fe" }
                            }
                            
                            transform: Rotation {
                                origin.x: card.width / 2
                                origin.y: card.height / 2
                                axis { x: 0; y: 1; z: 0 }
                                angle: 180
                            }
                            
                            Column {
                                anchors.centerIn: parent
                                spacing: 15
                                
                                Text {
                                    text: "详细信息"
                                    font.pixelSize: 20
                                    color: "white"
                                    font.bold: true
                                    anchors.horizontalCenter: parent.horizontalCenter
                                }
                                
                                Text {
                                    text: "这是卡片的背面\n包含更多内容"
                                    font.pixelSize: 14
                                    color: "white"
                                    horizontalAlignment: Text.AlignHCenter
                                }
                            }
                        }
                        
                        MouseArea {
                            anchors.fill: parent
                            hoverEnabled: true
                            
                            onClicked: {
                                parent.parent.flipped = !parent.parent.flipped
                            }
                            
                            onEntered: {
                                scaleUpAnimation.start()
                            }
                            
                            onExited: {
                                scaleDownAnimation.start()
                            }
                        }
                        
                        states: [
                            State {
                                name: "flipped"
                                when: parent.flipped
                                PropertyChanges {
                                    target: rotation
                                    angle: 180
                                }
                            }
                        ]
                        
                        transitions: [
                            Transition {
                                NumberAnimation {
                                    target: rotation
                                    property: "angle"
                                    duration: 600
                                    easing.type: Easing.InOutQuad
                                }
                            }
                        ]
                        
                        NumberAnimation {
                            id: scaleUpAnimation
                            target: scale
                            properties: "xScale,yScale"
                            to: 1.1
                            duration: 200
                        }
                        
                        NumberAnimation {
                            id: scaleDownAnimation
                            target: scale
                            properties: "xScale,yScale"
                            to: 1.0
                            duration: 200
                        }
                    }
                }
            }
        }
        
        Text {
            anchors.bottom: parent.bottom
            anchors.horizontalCenter: parent.horizontalCenter
            anchors.bottomMargin: 30
            text: "点击卡片翻转 | 悬停放大"
            font.pixelSize: 18
            color: "white"
            opacity: 0.8
        }
    }
}
```

**学习要点:**
- 3D 变换
- 状态和过渡
- 鼠标悬停效果
- 复杂动画组合

---

## 📝 运行这些示例

1. 创建新的 QML 文件
2. 复制代码
3. 在 Qt Creator 中运行
4. 尝试修改参数看效果

## 🎯 练习建议

1. **修改参数** - 改变颜色、大小、速度
2. **添加功能** - 增加新的交互
3. **组合效果** - 将多个效果结合
4. **创造新游戏** - 基于这些示例创作

继续下一部分... →
