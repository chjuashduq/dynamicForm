# QML 完全学习教程 - 第2章：布局系统详解

## 📚 目录
1. [定位器 (Positioners)](#定位器-positioners)
2. [锚点布局 (Anchors)](#锚点布局-anchors)
3. [布局管理器 (Layouts)](#布局管理器-layouts)
4. [实战案例](#实战案例)

---

## 定位器 (Positioners)

定位器是简单的容器,自动排列子元素。

### 1. Row - 水平排列

```qml
import QtQuick 6.5

Row {
    spacing: 10  // 元素间距
    
    Rectangle { width: 50; height: 50; color: "red" }
    Rectangle { width: 50; height: 50; color: "green" }
    Rectangle { width: 50; height: 50; color: "blue" }
}
```

**效果:** 🟥 🟩 🟦

**常用属性:**
- `spacing` - 元素间距
- `layoutDirection` - 布局方向 (LeftToRight / RightToLeft)
- `padding` - 内边距

### 2. Column - 垂直排列

```qml
Column {
    spacing: 10
    
    Rectangle { width: 100; height: 30; color: "red" }
    Rectangle { width: 100; height: 30; color: "green" }
    Rectangle { width: 100; height: 30; color: "blue" }
}
```

**效果:**
```
🟥
🟩
🟦
```

### 3. Grid - 网格排列

```qml
Grid {
    columns: 3      // 列数
    rows: 2         // 行数
    spacing: 10
    
    Repeater {
        model: 6
        Rectangle {
            width: 50
            height: 50
            color: Qt.rgba(Math.random(), Math.random(), Math.random(), 1)
            
            Text {
                text: index
                anchors.centerIn: parent
            }
        }
    }
}
```

**效果:**
```
🟥 🟩 🟦
🟨 🟪 🟧
```

### 4. Flow - 流式布局

自动换行的布局。

```qml
Flow {
    width: 200
    spacing: 10
    
    Repeater {
        model: 10
        Rectangle {
            width: 40
            height: 40
            color: "lightblue"
            border.color: "blue"
            
            Text {
                text: index
                anchors.centerIn: parent
            }
        }
    }
}
```

**特点:** 当一行放不下时自动换行

---

## 锚点布局 (Anchors)

锚点是 QML 最强大的布局方式,通过相对定位实现灵活布局。

### 1. 基本锚点

```qml
Rectangle {
    id: container
    width: 400
    height: 300
    color: "#f0f0f0"
    
    Rectangle {
        id: box
        width: 100
        height: 100
        color: "red"
        
        // 锚定到父元素的中心
        anchors.centerIn: parent
    }
}
```

### 2. 边缘锚点

```qml
Rectangle {
    id: container
    width: 400
    height: 300
    
    // 顶部栏
    Rectangle {
        id: topBar
        height: 50
        color: "#409EFF"
        
        // 锚定到父元素的顶部、左侧、右侧
        anchors.top: parent.top
        anchors.left: parent.left
        anchors.right: parent.right
    }
    
    // 底部栏
    Rectangle {
        id: bottomBar
        height: 50
        color: "#67C23A"
        
        // 锚定到父元素的底部、左侧、右侧
        anchors.bottom: parent.bottom
        anchors.left: parent.left
        anchors.right: parent.right
    }
    
    // 中间内容区
    Rectangle {
        color: "white"
        
        // 锚定到顶部栏下方和底部栏上方
        anchors.top: topBar.bottom
        anchors.bottom: bottomBar.top
        anchors.left: parent.left
        anchors.right: parent.right
    }
}
```

### 3. 填充锚点

```qml
Rectangle {
    id: parent
    width: 400
    height: 300
    
    Rectangle {
        // 填充整个父元素
        anchors.fill: parent
        color: "lightblue"
    }
}
```

### 4. 边距 (Margins)

```qml
Rectangle {
    id: container
    width: 400
    height: 300
    color: "#f0f0f0"
    
    Rectangle {
        color: "red"
        
        // 填充父元素,但留出边距
        anchors.fill: parent
        anchors.margins: 20          // 所有边距
        
        // 或者单独设置
        // anchors.topMargin: 10
        // anchors.bottomMargin: 10
        // anchors.leftMargin: 20
        // anchors.rightMargin: 20
    }
}
```

### 5. 水平和垂直居中

```qml
Rectangle {
    width: 400
    height: 300
    
    Rectangle {
        width: 100
        height: 100
        color: "red"
        
        // 水平居中
        anchors.horizontalCenter: parent.horizontalCenter
        
        // 垂直居中
        anchors.verticalCenter: parent.verticalCenter
        
        // 或者直接使用
        // anchors.centerIn: parent
    }
}
```

### 6. 相对于兄弟元素

```qml
Rectangle {
    width: 400
    height: 300
    
    Rectangle {
        id: box1
        width: 100
        height: 100
        color: "red"
        anchors.left: parent.left
        anchors.top: parent.top
        anchors.margins: 20
    }
    
    Rectangle {
        id: box2
        width: 100
        height: 100
        color: "green"
        
        // 在 box1 右侧
        anchors.left: box1.right
        anchors.leftMargin: 10
        anchors.top: box1.top
    }
    
    Rectangle {
        id: box3
        width: 100
        height: 100
        color: "blue"
        
        // 在 box1 下方
        anchors.top: box1.bottom
        anchors.topMargin: 10
        anchors.left: box1.left
    }
}
```

---

## 布局管理器 (Layouts)

布局管理器提供更高级的布局功能,自动计算尺寸。

**需要导入:**
```qml
import QtQuick.Layouts 1.4
```

### 1. RowLayout - 水平布局

```qml
import QtQuick 6.5
import QtQuick.Layouts 1.4

RowLayout {
    spacing: 10
    
    Rectangle {
        color: "red"
        Layout.preferredWidth: 100
        Layout.preferredHeight: 50
    }
    
    Rectangle {
        color: "green"
        Layout.fillWidth: true      // 填充剩余空间
        Layout.preferredHeight: 50
    }
    
    Rectangle {
        color: "blue"
        Layout.preferredWidth: 100
        Layout.preferredHeight: 50
    }
}
```

**Layout 属性:**
- `Layout.preferredWidth` - 首选宽度
- `Layout.preferredHeight` - 首选高度
- `Layout.minimumWidth` - 最小宽度
- `Layout.maximumWidth` - 最大宽度
- `Layout.fillWidth` - 填充宽度
- `Layout.fillHeight` - 填充高度
- `Layout.alignment` - 对齐方式

### 2. ColumnLayout - 垂直布局

```qml
ColumnLayout {
    spacing: 10
    anchors.fill: parent
    
    Rectangle {
        color: "red"
        Layout.preferredHeight: 50
        Layout.fillWidth: true
    }
    
    Rectangle {
        color: "green"
        Layout.fillHeight: true     // 填充剩余高度
        Layout.fillWidth: true
    }
    
    Rectangle {
        color: "blue"
        Layout.preferredHeight: 50
        Layout.fillWidth: true
    }
}
```

### 3. GridLayout - 网格布局

```qml
GridLayout {
    columns: 3
    rows: 2
    columnSpacing: 10
    rowSpacing: 10
    
    Rectangle { color: "red"; Layout.fillWidth: true; Layout.fillHeight: true }
    Rectangle { color: "green"; Layout.fillWidth: true; Layout.fillHeight: true }
    Rectangle { color: "blue"; Layout.fillWidth: true; Layout.fillHeight: true }
    Rectangle { color: "yellow"; Layout.fillWidth: true; Layout.fillHeight: true }
    Rectangle { color: "purple"; Layout.fillWidth: true; Layout.fillHeight: true }
    Rectangle { color: "orange"; Layout.fillWidth: true; Layout.fillHeight: true }
}
```

**跨行跨列:**
```qml
GridLayout {
    columns: 3
    
    Rectangle {
        color: "red"
        Layout.columnSpan: 2    // 跨2列
        Layout.fillWidth: true
        Layout.preferredHeight: 50
    }
    
    Rectangle {
        color: "green"
        Layout.fillWidth: true
        Layout.preferredHeight: 50
    }
    
    Rectangle {
        color: "blue"
        Layout.rowSpan: 2       // 跨2行
        Layout.fillHeight: true
        Layout.preferredWidth: 50
    }
}
```

---

## 实战案例

### 案例 1: 经典三栏布局

```qml
import QtQuick 6.5
import QtQuick.Controls 6.5

ApplicationWindow {
    width: 800
    height: 600
    visible: true
    title: "三栏布局"
    
    Row {
        anchors.fill: parent
        
        // 左侧边栏
        Rectangle {
            width: 200
            height: parent.height
            color: "#2c3e50"
            
            Column {
                anchors.fill: parent
                anchors.margins: 10
                spacing: 5
                
                Text {
                    text: "侧边栏"
                    color: "white"
                    font.pixelSize: 18
                    font.bold: true
                }
                
                Repeater {
                    model: ["首页", "产品", "关于", "联系"]
                    
                    Rectangle {
                        width: parent.width
                        height: 40
                        color: "#34495e"
                        radius: 4
                        
                        Text {
                            text: modelData
                            color: "white"
                            anchors.centerIn: parent
                        }
                        
                        MouseArea {
                            anchors.fill: parent
                            hoverEnabled: true
                            onEntered: parent.color = "#3498db"
                            onExited: parent.color = "#34495e"
                        }
                    }
                }
            }
        }
        
        // 中间内容区
        Rectangle {
            width: parent.width - 400
            height: parent.height
            color: "white"
            
            Column {
                anchors.fill: parent
                
                // 顶部栏
                Rectangle {
                    width: parent.width
                    height: 60
                    color: "#3498db"
                    
                    Text {
                        text: "内容区域"
                        color: "white"
                        font.pixelSize: 20
                        font.bold: true
                        anchors.centerIn: parent
                    }
                }
                
                // 内容
                Rectangle {
                    width: parent.width
                    height: parent.height - 60
                    color: "#ecf0f1"
                    
                    Text {
                        text: "这里是主要内容区域"
                        anchors.centerIn: parent
                        font.pixelSize: 16
                    }
                }
            }
        }
        
        // 右侧边栏
        Rectangle {
            width: 200
            height: parent.height
            color: "#95a5a6"
            
            Text {
                text: "右侧栏"
                anchors.centerIn: parent
                font.pixelSize: 16
            }
        }
    }
}
```

### 案例 2: 响应式表单布局

```qml
import QtQuick 6.5
import QtQuick.Controls 6.5
import QtQuick.Layouts 1.4

ApplicationWindow {
    width: 500
    height: 400
    visible: true
    title: "表单布局"
    
    ScrollView {
        anchors.fill: parent
        anchors.margins: 20
        
        GridLayout {
            columns: 2
            columnSpacing: 15
            rowSpacing: 15
            width: parent.width
            
            // 用户名
            Label { text: "用户名:" }
            TextField {
                placeholderText: "请输入用户名"
                Layout.fillWidth: true
            }
            
            // 密码
            Label { text: "密码:" }
            TextField {
                placeholderText: "请输入密码"
                echoMode: TextInput.Password
                Layout.fillWidth: true
            }
            
            // 邮箱
            Label { text: "邮箱:" }
            TextField {
                placeholderText: "请输入邮箱"
                Layout.fillWidth: true
            }
            
            // 性别
            Label { text: "性别:" }
            Row {
                spacing: 20
                RadioButton { text: "男" }
                RadioButton { text: "女" }
            }
            
            // 爱好
            Label { 
                text: "爱好:" 
                Layout.alignment: Qt.AlignTop
            }
            Column {
                spacing: 10
                CheckBox { text: "阅读" }
                CheckBox { text: "运动" }
                CheckBox { text: "音乐" }
            }
            
            // 简介
            Label { 
                text: "简介:" 
                Layout.alignment: Qt.AlignTop
            }
            TextArea {
                placeholderText: "请输入个人简介"
                Layout.fillWidth: true
                Layout.preferredHeight: 100
            }
            
            // 按钮
            Item { Layout.fillWidth: true }
            Row {
                spacing: 10
                Button { text: "提交" }
                Button { text: "重置" }
            }
        }
    }
}
```

### 案例 3: 卡片网格布局

```qml
import QtQuick 6.5
import QtQuick.Controls 6.5

ApplicationWindow {
    width: 800
    height: 600
    visible: true
    title: "卡片布局"
    
    ScrollView {
        anchors.fill: parent
        anchors.margins: 20
        
        Grid {
            columns: 3
            spacing: 20
            
            Repeater {
                model: 9
                
                Rectangle {
                    width: 240
                    height: 200
                    color: "white"
                    radius: 8
                    border.color: "#e0e0e0"
                    border.width: 1
                    
                    Column {
                        anchors.fill: parent
                        anchors.margins: 15
                        spacing: 10
                        
                        Rectangle {
                            width: parent.width
                            height: 100
                            color: Qt.rgba(Math.random(), Math.random(), Math.random(), 0.3)
                            radius: 4
                            
                            Text {
                                text: "图片 " + (index + 1)
                                anchors.centerIn: parent
                                font.pixelSize: 16
                            }
                        }
                        
                        Text {
                            text: "卡片标题 " + (index + 1)
                            font.pixelSize: 16
                            font.bold: true
                        }
                        
                        Text {
                            text: "这是卡片的描述文字..."
                            font.pixelSize: 12
                            color: "#666"
                            wrapMode: Text.WordWrap
                            width: parent.width
                        }
                    }
                    
                    // 悬停效果
                    MouseArea {
                        anchors.fill: parent
                        hoverEnabled: true
                        onEntered: parent.scale = 1.05
                        onExited: parent.scale = 1.0
                    }
                    
                    Behavior on scale {
                        NumberAnimation { duration: 200 }
                    }
                }
            }
        }
    }
}
```

---

## 💡 布局选择指南

| 场景 | 推荐布局 | 原因 |
|------|---------|------|
| 简单的水平/垂直排列 | Row/Column | 简单直接 |
| 需要自适应大小 | RowLayout/ColumnLayout | 自动计算尺寸 |
| 复杂的相对定位 | Anchors | 最灵活 |
| 表格式布局 | GridLayout | 对齐整齐 |
| 流式布局 | Flow | 自动换行 |

---

## 📝 小结

本章学习了:
- ✅ 定位器的使用 (Row, Column, Grid, Flow)
- ✅ 锚点布局的强大功能
- ✅ 布局管理器的高级特性
- ✅ 实际项目中的布局案例

**下一章预告:** 信号与槽 - 学习 QML 的事件处理机制
